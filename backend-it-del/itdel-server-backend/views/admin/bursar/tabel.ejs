<table id="example2" class="table table-bordered table-hover">
    <thead>
    <tr>
      <th>Judul</th>
      <th>Deskripsi</th>
      <th>Thumbnial</th>
      <th>Aksi</th>
    </tr>
    </thead>
    <tbody>
        <% for(let i = 0; i < bursar.length; i++) { %>
    <tr>
        <td><%= bursar[i].title %></td>
        <td><%= bursar[i].description %></td>
        <td>
            <img src="images/<%= bursar[i].thumbnial %>" alt="Thumbnial" width="200px">
        </td>
        <td>
            <form action="/bursar/delete/<%= bursar[i]._id %>?_method=DELETE" method="post">
                <a href="/bursar/edit/<%= bursar[i]._id %>" class="btn btn-info btn-sm"><i class="fas fa-pencil-alt"></i> Ubah</a>
                <button class="btn btn-danger btn-sm"><i class="fas fa-trash-alt"></i> Hapus</button>
            </form>
        </td>
    </tr>
    <% } %>
    </tbody>
  </table>