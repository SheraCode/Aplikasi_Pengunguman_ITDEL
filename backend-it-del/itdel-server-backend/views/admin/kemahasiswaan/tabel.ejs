<table id="example2" class="table table-bordered table-hover">
    <thead>
    <tr>
      <th>Judul</th>
      <th>Deskripsi</th>
      <th>Thumbnial</th>
      <th>Aksi</th>
    </tr>
    </thead>
    <tbody>
        <% for(let i = 0; i < kemahasiswaan.length; i++) { %>
    <tr>
        <td><%= kemahasiswaan[i].title %></td>
        <td><%= kemahasiswaan[i].description %></td>
        <td>
            <img src="images/<%= kemahasiswaan[i].thumbnial %>" alt="Thumbnial" width="200px">
        </td>
        <td>
            <form action="/kemahasiswaan/delete/<%= kemahasiswaan[i]._id %>?_method=DELETE" method="post">
                <a href="/kemahasiswaan/edit/<%= kemahasiswaan[i]._id %>" class="btn btn-info btn-sm"><i class="fas fa-pencil-alt"></i> Ubah</a>
                <button class="btn btn-danger btn-sm"><i class="fas fa-trash-alt"></i> Hapus</button>
            </form>
        </td>
    </tr>
    <% } %>
    </tbody>
  </table>